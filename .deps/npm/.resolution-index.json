{
  "contracts/Vote.sol": {
    "./CandidateRegistry.sol": "contracts/CandidateRegistry.sol",
    "./VoterRegistry.sol": "contracts/VoterRegistry.sol",
    "__sources__": {
      "contracts/Vote.sol": {
        "content": "// SPDX-License-Identifier: MIT\r\npragma solidity ^0.8.28;\r\n\r\n// Імпортуємо попередні реєстри кандидатів та виборців\r\nimport \"./CandidateRegistry.sol\";\r\nimport \"./VoterRegistry.sol\";\r\n\r\n// Керуючий контракт успадковує логіку обох реєстрів\r\ncontract Vote is CandidateRegistry, VoterRegistry {\r\n    \r\n    // 1. Еnum для опису етапів (станів) голосування\r\n    enum Status {\r\n        NotStarted, // Голосування ще не почалося (йде реєстрація)\r\n        Active,     // Голосування активне (можна голосувати)\r\n        Finished    // Голосування завершено\r\n    }\r\n\r\n    // 2. Змінна стану голосування\r\n    Status public status;\r\n\r\n    // 3. Події для логування життєвого циклу голосування\r\n    event VotingStarted();\r\n    event VoteCast(address indexed voter, uint indexed candidateId);\r\n    event VotingFinished();\r\n\r\n    // 4. Модифікатор, який дозволяє голосувати ТІЛЬКИ коли статус Active\r\n    modifier onlyDuringVoting() {\r\n        require(status == Status.Active, \"Voting is not active\");\r\n        _;\r\n    }\r\n\r\n    // 5. Функція додавання кандидатів власником (тільки ДО початку голосування)\r\n    function addCandidateToVote(string memory name) external onlyOwner {\r\n        require(status == Status.NotStarted, \"Cannot add candidates after start\");\r\n        addCandidate(name); // Викликаємо внутрішню функцію з CandidateRegistry\r\n    }\r\n\r\n    // Функція реєстрації виборців власником (тільки ДО початку голосування)\r\n    function registerVoterToVote(address voterAddress) external onlyOwner {\r\n        require(status == Status.NotStarted, \"Cannot register voters after start\");\r\n        _registerVoter(voterAddress); // Викликаємо внутрішню функцію з VoterRegistry\r\n    }\r\n\r\n    // 6. Функція запуску голосування власником\r\n    function startVoting() external onlyOwner {\r\n        require(status == Status.NotStarted, \"Voting already started or finished\");\r\n        status = Status.Active;\r\n        emit VotingStarted();\r\n    }\r\n\r\n    // 7. Функція завершення голосування власником\r\n    function finishVoting() external onlyOwner onlyDuringVoting {\r\n        status = Status.Finished;\r\n        emit VotingFinished();\r\n    }\r\n\r\n    // 8. Публічна функція для голосування користувачами\r\n    function vote(uint candidateId) external onlyDuringVoting {\r\n        // Отримуємо посилання на структуру виборця у пам'яті storage для її зміни\r\n        Voter storage senderVoter = voters[msg.sender];\r\n\r\n        // Перевірка А: Чи зареєстрований цей акаунт власником проекту\r\n        require(senderVoter.isRegistered, \"You are not registered to vote\");\r\n        \r\n        // Перевірка Б: Чи не голосував цей користувач раніше\r\n        require(!senderVoter.hasVoted, \"You have already voted\");\r\n\r\n        // Логіка зміни стану:\r\n        senderVoter.hasVoted = true;      // Позначаємо, що користувач проголосував\r\n        voteCandidate(candidateId);        // Зараховуємо голос кандидату через CandidateRegistry\r\n\r\n        // Генеруємо подію успішного голосування\r\n        emit VoteCast(msg.sender, candidateId);\r\n    }\r\n}",
        "file": "contracts/Vote.sol"
      },
      "contracts/CandidateRegistry.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28;\n\nimport \"./Ownable.sol\";\n\nabstract contract CandidateRegistry is Ownable {\n    // 1. Структура кандидата\n    struct Candidate {\n        uint id;\n        string name;\n        uint votes;\n    }\n\n    // 2. Динамічний масив для зберігання всіх кандидатів\n    Candidate[] public candidates;\n\n    // 3. Подія, яка викликається при додаванні нового кандидата\n    event CandidateAdded(uint id, string name);\n\n    // 4. Внутрішня (internal) функція для додавання кандидата\n    // Вона доступна тільки всередині цього контракту та його нащадків\n    function addCandidate(string memory name) internal {\n        uint newId = candidates.length; // ID дорівнює поточному індексу в масиві\n        \n        // Створюємо та додаємо нового кандидата (початкові голоси = 0)\n        candidates.push(Candidate(newId, name, 0));\n        \n        // Генеруємо подію додавання\n        emit CandidateAdded(newId, name);\n    }\n\n    // 5. Внутрішня функція для збільшення кількості голосів кандидата\n    function voteCandidate(uint id) internal {\n        // Перевіряємо, що такий кандидат взагалі існує в масиві\n        require(id < candidates.length, \"Candidate does not exist\");\n        candidates[id].votes++;\n    }\n\n    // 6. Публічна функція для отримання інформації про кандидата за його ID\n    function getCandidate(uint id) external view returns (uint, string memory, uint) {\n        require(id < candidates.length, \"Candidate does not exist\");\n        Candidate memory c = candidates[id];\n        return (c.id, c.name, c.votes);\n    }\n\n    // 7. Публічна функція, що повертає загальну кількість кандидатів\n    function totalCandidates() external view returns (uint) {\n        return candidates.length;\n    }\n}",
        "file": "contracts/CandidateRegistry.sol"
      },
      "contracts/Ownable.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28; // Використовуємо версію як у вашому Counter.sol\n\nabstract contract Ownable {\n    // 1. Змінна для зберігання адреси власника\n    address public owner;\n\n    // 2. Подія, яка спрацьовує при зміні або встановленні власника\n    event OwnerChanged(address indexed oldOwner, address indexed newOwner);\n\n    // 3. Конструктор встановлює першого власника (того, хто робить деплой)\n    constructor() {\n        owner = msg.sender;\n        emit OwnerChanged(address(0), msg.sender);\n    }\n\n    // 4. Модифікатор, який обмежує доступ до функцій (лише для owner)\n    modifier onlyOwner() {\n        require(msg.sender == owner, \"Not an owner\");\n        _;\n    }\n}",
        "file": "contracts/Ownable.sol"
      },
      "contracts/VoterRegistry.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28;\n\nimport \"./Ownable.sol\";\n\nabstract contract VoterRegistry is Ownable {\n    // 1. Структура виборця\n    struct Voter {\n        uint id;\n        bool isRegistered; // Прапорець: чи зареєстрований виборець admin-ом\n        bool hasVoted;     // Прапорець: чи вже віддав він свій голос\n    }\n\n    // Лічильник для створення унікальних ID виборців\n    uint public voterCount;\n\n    // 2. Mapping для зберігання інформації про виборців за їхніми адресами\n    mapping(address => Voter) public voters;\n\n    // 3. Подія реєстрації нового виборця\n    event VoterRegistered(address indexed voterAddress, uint id);\n\n    // 4. Внутрішня функція реєстрації виборця\n    function _registerVoter(address voterAddress) internal {\n        // Перевіряємо, щоб адреса не була нульовою і виборець не був зареєстрований раніше\n        require(voterAddress != address(0), \"Invalid voter address\");\n        require(!voters[voterAddress].isRegistered, \"Voter already registered\");\n\n        voterCount++; // Збільшуємо лічильник для нового ID\n        \n        // Записуємо дані у mapping\n        voters[voterAddress] = Voter({\n            id: voterCount,\n            isRegistered: true,\n            hasVoted: false\n        });\n\n        // Генеруємо подію реєстрації\n        emit VoterRegistered(voterAddress, voterCount);\n    }\n}\n",
        "file": "contracts/VoterRegistry.sol"
      }
    }
  },
  "contracts/CandidateRegistry.sol": {
    "./Ownable.sol": "contracts/Ownable.sol"
  },
  "contracts/Voting.sol": {
    "./CandidateRegistry.sol": "contracts/CandidateRegistry.sol",
    "./VoterRegistry.sol": "contracts/VoterRegistry.sol",
    "__sources__": {
      "contracts/Voting.sol": {
        "content": "// SPDX-License-Identifier: MIT\r\npragma solidity ^0.8.28;\r\n\r\nimport \"./CandidateRegistry.sol\";\r\nimport \"./VoterRegistry.sol\";\r\n\r\n// Контракт Voting (успадковує CandidateRegistry та VoterRegistry)\r\ncontract Voting is CandidateRegistry, VoterRegistry {\r\n    \r\n    // 1. Enum Status, який описує стани голосування\r\n    enum Status {\r\n        NotStarted,\r\n        Active,\r\n        Finished\r\n    }\r\n\r\n    // 2. Змінна status для збереження поточного стану голосування\r\n    Status public status;\r\n\r\n    // 3. Події для логування ключових дій системи\r\n    event VotingStarted();\r\n    event VoteCast(address indexed voter, uint indexed candidateId);\r\n    event VotingFinished();\r\n\r\n    // 4. Модифікатор onlyDuringVoting (дозволяє функції лише під час активного голосування)\r\n    modifier onlyDuringVoting() {\r\n        require(status == Status.Active, \"Voting is not active\");\r\n        _;\r\n    }\r\n\r\n    // 5. Функція addCandidateToVote (доступна лише власнику, дозволяє додавати кандидатів лише до початку голосування)\r\n    function addCandidateToVote(string memory name) external onlyOwner {\r\n        require(status == Status.NotStarted, \"Cannot add candidates after start\");\r\n        addCandidate(name); // Викликає внутрішню функцію з CandidateRegistry\r\n    }\r\n\r\n    // Додаткова функція зв'язку для реєстрації виборців власником до початку голосування\r\n    function registerVoterToVote(address voterAddress) external onlyOwner {\r\n        require(status == Status.NotStarted, \"Cannot register voters after start\");\r\n        _registerVoter(voterAddress); // Викликає внутрішню функцію з VoterRegistry\r\n    }\r\n\r\n    // 6. Функція startVoting (запускає голосування, доступна лише для власнику, без повторного запуску)\r\n    function startVoting() external onlyOwner {\r\n        require(status == Status.NotStarted, \"Voting already started or finished\");\r\n        status = Status.Active;\r\n        emit VotingStarted();\r\n    }\r\n\r\n    // 7. Функція finishVoting (завершує голосування, доступна лише для власнику, працює лише під час активного стану)\r\n    function finishVoting() external onlyOwner onlyDuringVoting {\r\n        status = Status.Finished;\r\n        emit VotingFinished();\r\n    }\r\n\r\n    // 8. Функція vote (головна функція для прийому голосів)\r\n    function vote(uint candidateId) external onlyDuringVoting {\r\n        // Перевіряє, що виборець зареєстрований (викликає функцію з п. 6 контракту VoterRegistry)\r\n        require(isRegisteredVoter(msg.sender), \"You are not registered to vote\");\r\n        \r\n        // Перевіряє, що виборець ще не голосував\r\n        require(!voters[msg.sender].hasVoted, \"You have already voted\");\r\n\r\n        // Позначає виборця як такого, що вже проголосував (викликає функцію з п. 5 контракту VoterRegistry)\r\n        markAsVotedVoter(msg.sender); \r\n        \r\n        // Зараховує голос вибраному кандидату (викликає функцію з п. 5 контракту CandidateRegistry)\r\n        voteCandidate(candidateId); \r\n\r\n        // Генерує подію про успішне голосування\r\n        emit VoteCast(msg.sender, candidateId);\r\n    }\r\n}",
        "file": "contracts/Voting.sol"
      },
      "contracts/CandidateRegistry.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28;\n\nimport \"./Ownable.sol\";\n\nabstract contract CandidateRegistry is Ownable {\n    // 1. Структура кандидата\n    struct Candidate {\n        uint id;\n        string name;\n        uint votes;\n    }\n\n    // 2. Масив кандидатів\n    Candidate[] public candidates;\n\n    // 3. Подія, яка викликається при додаванні нового кандидата\n    event CandidateAdded(uint id, string name);\n\n    // 4. Внутрішня (internal) функція для додавання кандидата\n    function addCandidate(string memory name) internal {\n        uint newId = candidates.length; // автоматично присвоює йому ідентифікатор\n        \n        // встановлює початкову кількість голосів\n        candidates.push(Candidate(newId, name, 0));\n        \n        // генерує відповідну подію\n        emit CandidateAdded(newId, name);\n    }\n\n    // 5. Внутрішню функцію voteCandidate(...), яка збільшує кількість голосів кандидата\n    function voteCandidate(uint id) internal {\n        // Перевіряємо, що такий кандидат взагалі існує в масиві\n        require(id < candidates.length, \"Candidate does not exist\");\n        candidates[id].votes++;\n    }\n\n    // 6. Функцію getCandidate(...) для отримання інформації про кандидата\n    function getCandidate(uint id) external view returns (uint, string memory, uint) {\n        require(id < candidates.length, \"Candidate does not exist\");\n        Candidate memory c = candidates[id];\n        return (c.id, c.name, c.votes);\n    }\n\n    // 7. Функцію totalCandidates(), яка повертає кількість кандидатів\n    function totalCandidates() external view returns (uint) {\n        return candidates.length;\n    }\n}",
        "file": "contracts/CandidateRegistry.sol"
      },
      "contracts/Ownable.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28; \n\nabstract contract Ownable {\n    // 1. Змінна для зберігання адреси власника контракту\n    address public owner;\n\n    // 2. Подія, яка спрацьовує при зміні або встановленні власника\n    event OwnerChanged(address indexed oldOwner, address indexed newOwner);\n\n    // 3. Конструктор який\n    constructor() {\n        owner = msg.sender;     // встановлює власником адресу, що деплоїла контракт (msg.sender)\n        emit OwnerChanged(address(0), msg.sender);  // генерує подію OwnerChanged\n    }\n\n    // 4. Модифікатор onlyOwner, який дозволяє викликати певні функції лише власнику контракту\n    modifier onlyOwner() {\n        require(msg.sender == owner, \"Not an owner\");\n        _;\n    }\n}",
        "file": "contracts/Ownable.sol"
      },
      "contracts/VoterRegistry.sol": {
        "content": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.28;\n\nimport \"./Ownable.sol\";\n\nabstract contract VoterRegistry is Ownable {\n    // 1. Структура виборця\n    struct Voter {\n        uint id;\n        bool isRegistered; // Чи зареєстрований виборець admin-ом\n        bool hasVoted;     // Чи вже віддав він свій голос\n    }\n\n    // Лічильник для створення унікальних ID виборців\n    uint public voterCount;\n\n    // 2. Mapping для зберігання інформації про виборців\n    mapping(address => Voter) public voters;\n\n    // 3. Подія реєстрації нового виборця\n    event VoterRegistered(address indexed voterAddress, uint id);\n\n    // 4. Внутрішня функція реєстрації виборця\n    function _registerVoter(address voterAddress) internal {\n        require(voterAddress != address(0), \"Invalid voter address\");\n        require(!voters[voterAddress].isRegistered, \"Voter already registered\");\n\n        voterCount++;\n        \n        voters[voterAddress] = Voter({\n            id: voterCount,\n            isRegistered: true,\n            hasVoted: false\n        });\n\n        emit VoterRegistered(voterAddress, voterCount);\n    }\n\n    // 5. Внутрішня функція для позначення виборця як такого, що проголосував\n    function markAsVotedVoter(address voterAddress) internal {\n        require(voters[voterAddress].isRegistered, \"Voter is not registered\");\n        voters[voterAddress].hasVoted = true;\n    }\n\n    //  6. Публічна view-функція для перевірки, чи зареєстрований виборець\n    function isRegisteredVoter(address voterAddress) public view returns (bool) {\n        return voters[voterAddress].isRegistered;\n    }\n}",
        "file": "contracts/VoterRegistry.sol"
      }
    }
  },
  "contracts/VoterRegistry.sol": {
    "./Ownable.sol": "contracts/Ownable.sol"
  }
}